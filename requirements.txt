# Versiones fijadas a las que de verdad se usaron para construir el indice y
# medir la calidad. Sin fijarlas, un `pip install -r` de dentro de tres meses
# instala otra cosa y las cifras de docs/MEDICIONES.md dejan de ser
# reproducibles sin que nadie se entere.
#
# Python: 3.12. No es una preferencia — PyTorch y sentence-transformers hacen
# segfault en el 3.14 del sistema. Ver requires-python en pyproject.toml.

# -- descarga e ingesta de fuentes --------------------------------------
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.2
tqdm==4.70.0
pypdf==6.16.2
python-docx==1.2.0
pyyaml==6.0.3

# -- indice y busqueda ---------------------------------------------------
# torch y transformers llegan como dependencia de sentence-transformers, pero
# se importan directamente en el codigo: se declaran para que su version sea
# una decision y no un efecto secundario.
sentence-transformers==6.0.0
torch==2.13.0
transformers==5.16.1
chromadb==1.5.9
numpy==2.5.2
huggingface_hub==1.29.0

# rank-bm25 se retiro: la parte lexica es SQLite FTS5 en disco (index/build_fts.py).
# Cargaba los 718k fragmentos en memoria y costaba ~8GB de RAM.

# -- generacion de respuestas -------------------------------------------
llama-cpp-python==0.3.35   # modelo propio, en proceso, sin depender de Ollama
anthropic==1.3.0           # IA externa opcional, con la clave del usuario

# -- servidor MCP --------------------------------------------------------
mcp==2.1.1

# -- fine-tuning (solo si se entrena; no hace falta para usar el programa) --
peft==0.20.0
trl==1.12.0
accelerate==1.14.0
datasets==5.0.1
sentencepiece==0.2.2
gguf==0.19.0
